# Project context

- Repo path: `C:\Users\benja\Desktop\ICH Mail sender\ICH-Mail-sender`
- Vercel project name: `ich-mail-sender`
- Vercel project id: `prj_r1CXaAiHPxsnkLNfwYT2oiHlV73Q`
- Vercel team id: `team_bbWPk6vIFMtC58pzeXgWAXXK`
- Stable production domain: `https://ich-mail-sender-benjafyl-1504s-projects.vercel.app`
- Local Vercel metadata file: `.vercel/project.json`

## Preferred flow

1. Verify current login with `npm run vercel:whoami`.
2. Refresh local project metadata with `npm run vercel:pull` when ids or settings may be stale.
3. Use `npm run vercel:deploy` for preview deployments.
4. Use `npm run vercel:deploy:prod` only when intentionally deploying production.

## Notes

- `NEXT_PUBLIC_APP_URL` should point to the stable project domain, not a hash deployment URL.
- Preview deployments may require Vercel Authentication.
- When the repo is already linked, prefer local metadata plus MCP over manual project discovery.
